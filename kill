echo starting...
PID_TO_KILL=$( ps aux | grep "node kanjiLearning.js" | grep -v grep | awk '{ print $2 }' )
echo PID $PID_TO_KILL
if [ -z "$PID_TO_KILL" ]
then
    echo Nothing to kill
else
    kill -9 $PID_TO_KILL
    echo Killed
fi
